alias oo="cd ~/vault && nvim"
alias ls="lsd"
alias commit='opencode run "commit my current work using git conventions. If I have done too many things for a single commit, make multiple commits. If a commit fails, abort this operation and let me know the commit failed."'

# Tmuxifier aliases
alias tl="tmuxifier load-session"
alias te="tmuxifier edit-session"
alias tn="tmuxifier load-session notes"
alias ti="tmuxifier load-session imagesbygigi"
alias tp="tmuxifier load-session portfolio"
